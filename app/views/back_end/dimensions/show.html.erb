<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @dimension.name %>
</p>

<%= link_to 'Edit', polymorphic_path([:edit, :back_end, @itemable, @dimension]), class: 'btn btn-primary' %>
<%= link_to 'Dimensions list', polymorphic_path([:back_end, @itemable, :dimensions]), class: 'btn btn-default' %>
